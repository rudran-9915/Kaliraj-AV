class Solution
{
    public long reverse_digit(long n)
    {
        long duplicate = n;
        long rev=0;
        while(n>0){
            long dig = n%10;
            rev = rev*10+dig;
            n=n/10;
        }
        return (long)rev;
    }
}